# Implement applications based on local deployed multimodal LLM on cpu devices

v0.0.1
- first implement Speech-to-text (STT, or ASR) application based on gemma4 or qwen3asr running on llama.cpp
- llama.cpp server instead of llama-cpp-python were adopted because of multimodal support currently.
